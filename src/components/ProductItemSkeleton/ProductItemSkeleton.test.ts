import { describe, it } from "vitest";
// This one could probably do with a snapshot test
describe("ProductItemSkeleton", () => {
  it.todo("passes accessibility tests");
  it.todo("passes html validation tests");
});
